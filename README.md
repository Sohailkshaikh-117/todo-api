# todo-api
A simple REST API built with Node.js and Express for managing todos. Supports creating, retrieving, updating, and deleting todos. Built as a learning project to understand Express routing, layered architecture, and REST conventions.
