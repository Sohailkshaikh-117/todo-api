const path = require('path');
const fs = require('fs');



// check if the db folder exist



// if folder not exist, create the folder

// check if the file exist

// if the file not exist, create the file and initialize with empty object
